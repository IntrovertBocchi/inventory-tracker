{
  "name": "inventory-tracker",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND,STRIPE -c blue,green,magenta \"cd backend && venv\\Scripts\\python.exe app.py\" \"cd frontend && npm run dev\" \"stripe listen --forward-to localhost:5000/webhooks/stripe\""
  },
  
  "devDependencies": {
    "concurrently": "^10.0.3",
    "sass": "^1.101.0"
  },
  "dependencies": {
    "framer-motion": "^12.42.2"
  }
}
